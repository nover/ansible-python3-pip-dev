# Ansible pyton3 pip dev role

Role allowing the provisioned machine to precompile pip packages to wheels that can be uploaded online.

the ansible-pyton3-pip role is using wheels produced by this Role
